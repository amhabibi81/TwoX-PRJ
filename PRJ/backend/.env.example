# JWT Secret Key
# Must be at least 32 characters long for security
# Generate a strong random string for production
JWT_SECRET=your-secret-key-must-be-at-least-32-characters-long-for-security

# Server Port
# Port number for the backend server (default: 5000)
PORT=5000

# Database Path
# Path to SQLite database file (relative or absolute)
# The directory will be created automatically if it doesn't exist
DATABASE_PATH=./data/database.sqlite

# Admin Emails (Optional)
# Comma-separated list of admin email addresses
# Users with these emails will have admin access
# Leave empty for demo mode (no admin access)
# ADMIN_EMAILS=admin@example.com,admin2@example.com

# Log Level (Optional)
# Logging level: debug, info, warn, error
# Default: info (production) or debug (development)
# LOG_LEVEL=info

# Node Environment (Optional)
# Environment: development, production, test
# Default: development
# NODE_ENV=development
